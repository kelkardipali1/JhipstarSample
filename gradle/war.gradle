apply plugin: "war"

bootWar {
    mainClassName = "com.thoughtworks.basicapp.BasicappApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
